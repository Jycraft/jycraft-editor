import chai from "chai";
let expect = chai.expect;

import {Connection} from "../connection.service";

describe("Connections manager", function () {
    it("can be imported", function () {
        expect(1).to.equal(2);
    });
});
